# Change YOLO model to yolov8n (nano - fastest)
#
# Usage:
#   cupertino-cli set-model config/commands/set_model_yolov8n.yaml

command: set_model
model_variant: "n"
input_size: 640
confidence: 0.5
iou_threshold: 0.5
